{
  "name": "ai-cup-22",
  "version": "1.0.1",
  "main": "main.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/main.js"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  }
}