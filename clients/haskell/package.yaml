name: aicup22
version: 1.0.1

dependencies:
  - base
  - containers
  - text
  - binary
  - bytestring
  - network
  - transformers

executables:
  aicup22:
    main: Main.hs
    source-dirs: src
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    default-extensions:
      - ScopedTypeVariables
      - TypeSynonymInstances
      - FlexibleInstances
      - NamedFieldPuns
      - DuplicateRecordFields